import { StyleSheet } from 'react-native';
import { rem } from '../../index.style';

const css = StyleSheet.create({
  connectorview: {
    flex: 1,
    marginRight: 0.3 * rem,
  },
});

export default css;
